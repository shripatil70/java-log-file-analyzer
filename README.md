# Java Log File Analyzer

A Core Java–based command-line tool to analyze server access logs, detect suspicious IP activity, and generate structured CSV reports.

## Features
- Analyze one or multiple log files in a single run
- Count HTTP status codes (200, 403, 404, 500, etc.)
- Track IP address activity
- Detect suspicious IPs based on failed request thresholds
- Export analysis results to CSV
- Packaged as an executable JAR

## Tech Stack
- Java (Core Java)
- File I/O
- Collections Framework
- CLI-based execution

## Project Structure
